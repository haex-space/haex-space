# Supabase Configuration
# Get these values from https://supabase.com/dashboard/project/_/settings/api

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-publishable-api-key

# Cloudflare Turnstile (Captcha)
# Get these values from https://dash.cloudflare.com/?to=/:account/turnstile
# 1. Create a new site in Cloudflare Turnstile dashboard
# 2. Add your domain (e.g., haex.space, localhost for dev)
# 3. Copy Site Key here
# 4. Copy Secret Key to Supabase Dashboard > Auth > Bot and Abuse Protection > Captcha

NUXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key

# Release Download Sync
# These are used for self-hosting release downloads instead of GitHub
# Generate random secrets with: openssl rand -hex 32

# Secret for GitHub webhook signature verification
# Set this in GitHub repo > Settings > Webhooks > Secret
GITHUB_WEBHOOK_SECRET=your-webhook-secret

# Secret for manual sync endpoint (POST /api/releases/sync)
# Use with header: x-sync-secret
RELEASE_SYNC_SECRET=your-sync-secret

# Storage path for release files (default: /data/releases)
# RELEASES_STORAGE_PATH=/data/releases
