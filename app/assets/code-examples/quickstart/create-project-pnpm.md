```bash
# Create a new Vite project
pnpm create vite my-extension --template vue-ts

# Navigate to project
cd my-extension

# Install dependencies
pnpm install

# Install the SDK
pnpm add @haex-space/vault-sdk

# Initialize extension structure
pnpm dlx haex init
```
