```bash
# Create a new Vite project
bun create vite my-extension --template vue-ts

# Navigate to project
cd my-extension

# Install dependencies
bun install

# Install the SDK
bun add @haex-space/vault-sdk

# Initialize extension structure
bunx haex init
```
