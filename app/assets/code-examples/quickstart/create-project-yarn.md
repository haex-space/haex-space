```bash
# Create a new Vite project
yarn create vite my-extension --template vue-ts

# Navigate to project
cd my-extension

# Install dependencies
yarn install

# Install the SDK
yarn add @haex-space/vault-sdk

# Initialize extension structure
yarn dlx haex init
```
